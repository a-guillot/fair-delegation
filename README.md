# fair-delegation

Implementation of the paper...

`npm install`
`cd blockchain`
`docker-compose down`
`docker-compose up --build`


In another terminal:
`cd scenario3`
`rm *js && tsc --EsModuleInterop *ts`
`docker-compose down`
`docker-compose up --build`
