# fair-delegation

Implementation of the paper...

`npm install`
`cd blockchain`
`docker-compose down`
`docker-compose up --build`


In another terminal:
`cd scenarioX`
`rm *js && tsc --EsModuleInterop *ts`
`docker-compose down`
`./stop.sh`
`docker-compose up --build`
